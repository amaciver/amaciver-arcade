# Sushi Scout environment variables
# Copy to .env and fill in your values

# Google Places API key (OPTIONAL - only for local testing without OAuth)
# The primary auth path uses Arcade Google OAuth, no API key needed.
# GOOGLE_PLACES_API_KEY=your_google_places_api_key_here

# Arcade API key (for Arcade platform access)
# ARCADE_API_KEY=your_arcade_api_key_here
