# Sushi Scout environment variables
# Copy to .env and fill in your values

# ---- Auth Mode ----
# Choose how search tools authenticate with Google Places API:
#   "api_key" (default) - uses GOOGLE_PLACES_API_KEY below
#   "oauth"             - uses custom Arcade OAuth2 provider (no API key needed)
# SUSHI_SCOUT_AUTH_MODE=api_key

# ---- API Key Mode (default) ----
# Get a key from: https://console.cloud.google.com (enable Places API New)
# GOOGLE_PLACES_API_KEY=your_google_places_api_key_here

# ---- OAuth Mode ----
# Requires a custom OAuth2 provider registered in Arcade. See README.md.
# GCP_PROJECT_ID=your_gcp_project_id

# ---- Arcade ----
# ARCADE_API_KEY=your_arcade_api_key_here
