# No secrets needed for the cat facts tool (get_cat_fact).
# Slack tools (meow_me, send_cat_fact) use Arcade's built-in Slack OAuth -
# authentication happens via browser, no tokens stored here.

# Arcade API key (for arcade login / platform access)
# ARCADE_API_KEY=your_arcade_key
