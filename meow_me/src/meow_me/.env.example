# meow_me environment variables
# Copy to meow_me/.env and fill in your values

# OpenAI (required for interactive agent + image generation)
# OPENAI_API_KEY=sk-...

# Slack auth — pick ONE:

#   Option A: Direct bot token (all features incl. image upload)
#   Requires --slack flag when running the agent: uv run python -m meow_me --slack
# SLACK_BOT_TOKEN=xoxb-...

#   Option B: Arcade OAuth (text messaging + avatars, no image upload)
# ARCADE_API_KEY=arc-...
# ARCADE_USER_ID=your-email@example.com
